# *Stand up an HTTP layer at all.** No `main.py`, no ASGI app, no routes exist. `FacialAnalysisPipeline`, `FacialScorer`, and `PlanService` are all real but fully disconnected from any entrypoint. This blocks every other item below.
2. **Wire `FacialAnalysisPipeline` into a real `/analyze` route** and pipe its output into `FacialScorer` → `PlanService`, so the one real end-to-end flow that exists in isolated classes actually runs as a request.
3. **Basic auth** (signup/login, token issuance) — currently zero identity concept exists, so there is no way to scope any data to a user at all.
4. **A persistence layer** — no DB models, no migrations exist. Without this, nothing computed can be saved, and "account ownership," "quota," and "plan history" are all meaningless.
5. **Enforce allergies and `avoid_ingredients`.** These fields are already captured into `PlanService`'s constraints dict (`plan_service.py:100-101`) but never read again — this is a live safety gap in the one recommendation path that exists: nothing currently stops a plan from recommending a product category a user is allergic to, because there is no ingredient-level data or check at all yet.
6. **Remove or explicitly gate automatic supplement recommendations.** `PlanService._build_supplement_recommendations` unconditionally recommends specific supplements and dosages (e.g., "Vitamin C (1000mg)") derived purely from photo priorities, with only a