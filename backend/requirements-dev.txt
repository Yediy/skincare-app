# Test-only additions on top of the production dependency set. Not
# installed in the Docker runtime image -- see requirements.txt.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.27.0
